"use strict";
// this is just a wrapper around parse5. We need a wrapper because parse5's
// types are quite bad. Both because they're not sensibly designed, and because
// they're out of date with its implementation.
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.traverse = exports.parse = void 0;
const parse5_1 = __importDefault(require("parse5"));
function parse(html) {
    return parse5_1.default.parse(html, { sourceCodeLocationInfo: true });
}
exports.parse = parse;
function traverse(node, fn) {
    if ('tagName' in node) {
        fn(node);
    }
    for (let child of node.childNodes) {
        if ('childNodes' in child) {
            traverse(child, fn);
        }
    }
}
exports.traverse = traverse;
//# sourceMappingURL=parse-html.js.map