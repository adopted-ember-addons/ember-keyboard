import Plugin from 'broccoli-plugin';
import { Bundler } from './bundler';
export default class CombineLazyChunks extends Plugin {
    private bundler;
    private targetFilename;
    constructor(bundler: Bundler, targetFilename: string);
    build(): Promise<void>;
}
